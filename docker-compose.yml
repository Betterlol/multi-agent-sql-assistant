services:
  sql-assistant:
    build: .
    container_name: multi-agent-sql-assistant
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./runtime_uploads:/app/runtime_uploads
    restart: unless-stopped
